#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../generate-schema/bin/generate-schema" "$@"
  ret=$?
else 
  node  "$basedir/../generate-schema/bin/generate-schema" "$@"
  ret=$?
fi
exit $ret
