extends layout

block content
    .w3-card-4
      header.w3-container.w3-teal
        h3 Repositórios

      if listRep
        ul
          each a in listRep
            li(style="padding-bottom:15px") 
              a(href=`/${a.id}`) #{a.id} - #{a.title}