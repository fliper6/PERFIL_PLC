extends layout

block content
    .w3-card-4
      header.w3-container.w3-teal
        h3 Indivíduo "#{indiv}"

      if dados
        each d in dados
          ul
            li(style="padding-bottom:10px")
              b #{d.p.value.split("#")[1]} : 
              if (d.o.value.split("#")[1])
                | #{d.o.value.split("#")[1]}
              else 
                | #{d.o.value} 
      else 
        div.w3-container
           p Nenhuma informação foi encontrada sobre este indivíduo.